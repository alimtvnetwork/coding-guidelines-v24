// Wrapper that re-exports the underlying component tagger plugin under a
// neutral name so the rest of the codebase does not depend on the upstream
// package's branding.
export { componentTagger } from "lovable-tagger";
