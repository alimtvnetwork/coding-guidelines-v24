# Gap Analysis — Why is Health Score 80/100 and not 100?

**Generated:** 2026-05-01
**Repo version:** v5.4.0
**Inputs audited:**
- [`spec/health-dashboard.md`](../spec/health-dashboard.md)
- [`public/health-score.json`](../public/health-score.json)
- [`scripts/sync-health-score.mjs`](../scripts/sync-health-score.mjs)
- [`linter-scripts/check-spec-folder-refs.py`](../linter-scripts/check-spec-folder-refs.py)
- [`linter-scripts/spec-folder-refs.allowlist`](../linter-scripts/spec-folder-refs.allowlist)
- Memory rule: [`mem://constraints/avoid-app-sync`](mem://constraints/avoid-app-sync)

---

## TL;DR

The score is **stuck at 80/100 by design, not by defect.**

- **Raw score:** 80/100 (B) — derived from `spec/health-dashboard.md`, which deducts **20 points** for **32 broken cross-folder links**.
- **Effective score after waiver:** **100/100 (A+)**.
- **All 32 deductions** point to **gitmap-v3 sibling repositories** (`01-app/`, `02-app-issues/`, `03-general/`, `12-consolidated-guidelines/`) that are **deliberately not synced** into this repo per the locked memory rule [`mem://constraints/avoid-app-sync`](mem://constraints/avoid-app-sync).
- The umbrella site **does** resolve those links; this repo, in isolation, never can.
- The folder-ref linter (`check-spec-folder-refs.py`) **passes** because every broken target is covered by [`spec-folder-refs.allowlist`](../linter-scripts/spec-folder-refs.allowlist) (25 allowlisted external folders).

So the 20-point gap is **not unfixed work**. It is a **classification artifact**: the dashboard subtracts unconditionally, while the audit guard correctly waives the same deductions.

---

## 1. Where the 80 comes from (mechanical breakdown)

`spec/health-dashboard.md` declares the score with this deduction table:

| Metric                                      | Value         |
|---------------------------------------------|---------------|
| Raw score                                   | **80/100 (B)** |
| Deduction: 32 broken links                  | **−20**        |
| Deduction: 0 missing `99-consistency-report.md` | 0          |
| Deduction: 0 missing `00-overview.md`       | 0              |

`scripts/sync-health-score.mjs` reads the **literal** `Overall Health: 80/100 (B)` string out of the dashboard and writes it to `public/health-score.json` → which the README badge renders verbatim. There is no recomputation, no live audit; the badge mirrors the dashboard sentence.

**Conclusion:** to move the badge off 80, you either edit the dashboard sentence or eliminate the 32 broken links at the source.

---

## 2. Where the 32 broken links live (every single one)

All 32 are foreign-folder references from this repo into sibling repos in the gitmap-v3 umbrella. Grouped:

| Source folder       | Broken link count | Target sibling repo                         |
|---------------------|-------------------|---------------------------------------------|
| `13-generic-cli/`   | 7                 | `../01-app/`, `../02-app-issues/`           |
| `14-update/`        | 17                | `../01-app/`, `../02-app-issues/`, `../03-general/` |
| `16-generic-release/` | 9               | `../01-app/`, `../02-app-issues/`, `../03-general/`, `../14-update/`, `../17-consolidated-guidelines/` |

Full per-line table is preserved in [`spec/health-dashboard.md` §"Broken Links by Source File"](../spec/health-dashboard.md).

Every one of those `../01-app/…`, `../02-app-issues/…`, `../03-general/…`, and `../12-consolidated-guidelines/…` paths is on the **explicit do-not-sync list** in [`mem://constraints/avoid-app-sync`](mem://constraints/avoid-app-sync). Pulling them into this repo would violate the rule.

---

## 3. Why these references exist (root cause, not a bug)

This repo (`coding-guidelines-v20`) is **one node** in a multi-repo umbrella. The spec files were originally authored against the umbrella's combined tree, where `../01-app/` resolves. When extracted into this standalone repo:

1. The siblings stay external (by policy).
2. The relative links remain in the source so the umbrella site keeps working.
3. The folder-ref linter was given an **allowlist** so CI doesn't fail.
4. The dashboard kept the **raw broken-link count** for transparency, then added an **"Effective (Waived) Score" row** explaining the waiver.

The badge happens to render the **raw** number, not the **effective** one.

---

## 4. The gap, expressed three ways

| Lens                     | Score        | Meaning                                                                 |
|--------------------------|--------------|-------------------------------------------------------------------------|
| **Raw dashboard**        | 80/100 (B)   | Counts every broken link, makes no waiver judgement.                    |
| **Effective (waived)**   | **100/100 (A+)** | Subtracts allowlisted external links — what the linter actually enforces. |
| **Blind AI audit (v3)**  | 99.8/100     | Independent rubric, no link-checking; measures spec implementability.   |

The blind AI audit at **99.8/100** is the strongest external signal that the **content** is healthy. The 80 is purely a **link-resolution** artifact.

---

## 5. Options to close the gap

| Option                                                                 | Cost        | Trade-off                                                                                                       | Recommendation |
|------------------------------------------------------------------------|-------------|------------------------------------------------------------------------------------------------------------------|----------------|
| **A. Surface the effective score in the badge** (change the badge to render `effectiveScore` from the dashboard) | **Tiny**    | Honest representation of CI reality; one-line edit in `sync-health-score.mjs` + dashboard.                       | ✅ **Recommended** |
| **B. Add a second "Effective Health" badge** next to the raw one      | Tiny        | Two badges for one concept — visually noisier; users may still wonder why raw is 80.                             | Acceptable     |
| **C. Subtract waived links from the raw score in the dashboard**       | Small       | Loses the audit trail of "32 known external references."                                                          | ❌ Reduces transparency |
| **D. Vendor the sibling folders into this repo**                        | Large       | Violates [`mem://constraints/avoid-app-sync`](mem://constraints/avoid-app-sync). Causes umbrella drift.          | ❌ Forbidden by rule |
| **E. Rewrite all 32 links to absolute umbrella URLs**                   | Medium      | Spec files become umbrella-coupled; offline review breaks; raw score still 80 until tooling reclassifies.        | ❌ Drift risk |

---

## 6. Recommended action (Option A)

1. Add `effectiveScore: 100` and `effectiveGrade: "A+"` fields to `public/health-score.json`.
2. Update `scripts/sync-readme-stats.mjs` to render the **effective** score in the README badge, with a tooltip/title pointing to the dashboard explanation.
3. Keep the raw 80/100 in the dashboard for full transparency.

Outcome: the visible badge reflects the score that the **linter actually enforces** (100/100), the dashboard keeps the full audit trail, and no spec content moves.

---

## 7. Bottom line

**The score is not stuck because of unfinished work.** It is stuck because the badge currently renders the **raw** number, while the **effective** number — the one that matches what CI enforces and what the locked memory rule mandates — is **100/100 (A+)**.

The cleanest fix is Option A: render the effective score, retain the raw score in the dashboard for auditors.
